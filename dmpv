#!/bin/sh -e

# A tiny script for launcing videos inside Videos dir with mpv
# No more need for a detached terminal taking up your screen space

# Can use fzf instead, and probably rofi, bemenu etc aswell
LAUNCHER="dmenu -i -l 50"

cd "$HOME"/Videos

mpv "$(stest -- * | $LAUNCHER)"
